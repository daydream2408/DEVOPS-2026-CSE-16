services:

  backend:
    build:
      context: ./backend
    container_name: options-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: options-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
